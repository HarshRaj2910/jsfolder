## What is React?
React is JS library.
USed for front end web development.
created and used by meta.
famous for work in virtual DOM.
React change in Virtual DOM.

## SPA(Single Page Application):-
 React is a web application that loads a single HTML page and dynamically updates the content using JS , without reqiring a full page reload during user interaction.

 ## JSX :- html always inside  return 
 ## function are first class citizens.
 ## variable are immutables.
 ## function have no side effects.
 ## Components :-
 Components are function for user  interface.
 Math function : 
 input x --> let y=f(x); ---> output number
 